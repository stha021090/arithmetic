parameters:

services:
  _defaults:
    autowire: true
    autoconfigure: true
    bind:
      $source: '%kernel.project_dir%/public/data/source'
      $resultFile: '%kernel.project_dir%/public/data/result/result.csv'
      $logFile: '%kernel.project_dir%/public/data/result/log.txt'

  App\:
    resource: '../src/'
